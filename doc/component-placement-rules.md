# Правила размещения компонентов в FSD архитектуре

## 📋 Чек-лист для принятия решения

Перед созданием нового компонента ответьте на вопросы:

1. **Используется ли компонент на нескольких страницах?**
   - ✅ Да → `shared/ui/` или `widgets/`
   - ❌ Нет → `pages/[page-name]/ui/`

2. **Связан ли компонент с конкретной страницей?**
   - ✅ Да → `pages/[page-name]/ui/`
   - ❌ Нет → проверьте следующий вопрос

3. **Является ли компонент крупным самостоятельным блоком?**
   - ✅ Да → `widgets/`
   - ❌ Нет → `shared/ui/`

4. **Имеет ли компонент префикс, связанный с конкретной страницей?**
   - ✅ Да (например, `ProjectHeader`, `ProjectFooter`) → `pages/projects/ui/`
   - ❌ Нет → `shared/ui/`

---

## 🎯 Правила размещения по слоям

### 1. `shared/ui/` — Переиспользуемые UI компоненты

**Критерии:**

- ✅ Используется на **2+ страницах** или в разных контекстах
- ✅ Не имеет привязки к конкретной бизнес-логике
- ✅ Универсальный компонент (кнопка, карточка, модалка, инпут)
- ✅ Не имеет префикса конкретной страницы/сущности

**Примеры правильного размещения:**

```
shared/ui/
  ├── button/          ✅ Используется везде
  ├── card/            ✅ Универсальная карточка
  ├── modal/           ✅ Модальное окно
  ├── input/           ✅ Поле ввода
  ├── navbar/          ✅ Навигация (используется на всех страницах)
  └── section-layout/  ✅ Универсальный layout для секций
```

**Примеры неправильного размещения:**

```
shared/ui/
  ├── project-header/           ❌ Используется только на странице проектов
  ├── project-content-section/  ❌ Специфично для страницы проектов
  └── project-footer/           ❌ Специфично для страницы проектов
```

---

### 2. `pages/[page-name]/ui/` — Компоненты конкретной страницы

**Критерии:**

- ✅ Используется **только на одной странице**
- ✅ Имеет префикс, связанный со страницей (например, `Project*`)
- ✅ Является частью структуры конкретной страницы
- ✅ Не переиспользуется в других местах

**Структура:**

```
pages/projects/
  ├── ui/
  │   ├── projectDetails.tsx           ✅ Основной компонент страницы
  │   ├── ProjectHeader.tsx            ✅ Заголовок страницы проекта
  │   ├── ProjectContentSection.tsx    ✅ Секция контента проекта
  │   ├── ProjectDescriptionSection.tsx ✅ Секция описания проекта
  │   ├── ProjectFooter.tsx            ✅ Футер страницы проекта
  │   └── ProjectNotFound.tsx          ✅ Компонент "проект не найден"
  ├── model/
  │   └── useProjectDetail.ts          ✅ Хук для получения данных
  └── index.tsx                        ✅ Экспорт страницы
```

**Правила именования:**

- Основной компонент страницы: `[PageName].tsx` (например, `projectDetails.tsx`)
- Компоненты страницы: `[PagePrefix][ComponentName].tsx` (например, `ProjectHeader.tsx`)
- Обертка страницы: `[PageName]Page` в `index.tsx`

---

### 3. `widgets/` — Крупные самостоятельные блоки

**Критерии:**

- ✅ Используется на **2+ страницах**
- ✅ Имеет собственную бизнес-логику
- ✅ Является крупным составным блоком
- ✅ Может содержать несколько компонентов и логику

**Примеры:**

```
widgets/
  ├── project-card/        ✅ Карточка проекта (используется в списках, превью)
  ├── project-theme/       ✅ Управление темами проектов
  ├── project-navigation/  ✅ Навигация между проектами
  └── header/             ✅ Шапка сайта (если используется на нескольких страницах)
```

**Когда НЕ использовать widgets:**

- ❌ Компонент используется только на одной странице → `pages/[page]/ui/`
- ❌ Простой переиспользуемый компонент без логики → `shared/ui/`

---

### 4. `entities/` — Бизнес-сущности

**Критерии:**

- ✅ Представляет бизнес-сущность (Project, User, Order)
- ✅ Содержит типы, данные, базовые хуки
- ✅ UI компоненты сущности должны быть переиспользуемыми

**Примеры:**

```
entities/project/
  ├── model/
  │   ├── type.ts      ✅ Типы проекта
  │   ├── data.ts      ✅ Данные проектов
  │   └── index.ts     ✅ Экспорты
  └── ui/
      └── ProjectCard.tsx  ✅ Карточка проекта (переиспользуемая)
```

---

## 🔍 Алгоритм принятия решения

```
Новый компонент
    │
    ├─ Используется на 2+ страницах?
    │   ├─ Да → Имеет бизнес-логику?
    │   │   ├─ Да → widgets/
    │   │   └─ Нет → shared/ui/
    │   │
    │   └─ Нет → pages/[page-name]/ui/
    │
    └─ Имеет префикс конкретной страницы?
        ├─ Да → pages/[page-name]/ui/
        └─ Нет → Проверь критерии выше
```

---

## ⚠️ Частые ошибки и как их избежать

### Ошибка 1: Компоненты страницы в `shared/ui/`

**Неправильно:**

```
shared/ui/project-header/  ❌
shared/ui/project-footer/   ❌
```

**Правильно:**

```
pages/projects/ui/ProjectHeader.tsx  ✅
pages/projects/ui/ProjectFooter.tsx  ✅
```

### Ошибка 2: Переиспользуемые компоненты в `pages/`

**Неправильно:**

```
pages/projects/ui/Button.tsx  ❌
pages/projects/ui/Modal.tsx   ❌
```

**Правильно:**

```
shared/ui/button/Button.tsx  ✅
shared/ui/modal/Modal.tsx    ✅
```

### Ошибка 3: Бизнес-логика в UI компонентах

**Неправильно:**

```tsx
// pages/projects/ui/projectDetails.tsx
export function ProjectDetail() {
  const { id } = useParams();  // ❌ Логика получения ID
  const projectData = { ... };  // ❌ Данные в UI
  const project = projectData[id];  // ❌ Логика поиска

  return <div>...</div>;
}
```

**Правильно:**

```tsx
// pages/projects/model/useProjectDetail.ts
export function useProjectDetail() {
  const { id } = useParams();
  const project = useMemo(() => projectData[id], [id]);
  return { project };
}

// pages/projects/ui/projectDetails.tsx
export function ProjectDetail() {
  const { project } = useProjectDetail(); // ✅ Логика в model
  return <div>...</div>;
}
```

---

## 📝 Правила именования

### Компоненты в `shared/ui/`

- Без префиксов: `Button`, `Card`, `Modal`
- Общие названия: `Navbar`, `SectionLayout`

### Компоненты в `pages/[page]/ui/`

- С префиксом страницы: `ProjectHeader`, `ProjectFooter`
- Основной компонент: `projectDetails.tsx` (camelCase)

### Компоненты в `widgets/`

- С префиксом функциональности: `ProjectCard`, `ProjectTheme`
- Описывает назначение виджета

---

## ✅ Чек-лист перед коммитом

Перед коммитом проверьте:

- [ ] Компонент находится в правильном слое?
- [ ] Компонент не имеет префикса конкретной страницы, если находится в `shared/`?
- [ ] Бизнес-логика вынесена в `model/` слой?
- [ ] Данные не захардкожены в UI компонентах?
- [ ] Именование соответствует правилам?
- [ ] Компонент экспортируется через `index.ts`?

---

## 🔄 Миграция существующих компонентов

Если нашли компонент не в том месте:

1. **Определите правильное место** по чек-листу выше
2. **Перенесите компонент** в правильную директорию
3. **Обновите импорты** во всех местах использования
4. **Удалите старый файл**
5. **Обновите экспорты** в `index.ts`

---

## 📚 Дополнительные ресурсы

- [FSD Architecture](fsd-architecture.md) - Общая архитектура проекта
- [Feature-Sliced Design](https://feature-sliced.design/) - Официальная документация FSD
